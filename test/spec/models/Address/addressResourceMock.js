/**
 * Created by Aliss on 11/08/2016.
 */

(function (angular) {
    'use strict';
    angular.module('lanceSolidario.address.addressResourceMock', []).service('addressResource', ['$q', function ($q) {
        var self = this;

        self.add = function (address) {
        };

        self.update = function (address) {
        };

    }])
})(angular);
